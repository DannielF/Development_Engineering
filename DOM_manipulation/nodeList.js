/**
 * @nodeList 
 * Similar to an array, but it is a list of nodes.
 * Has different methods than an array.
 */

const nodeListArray = [...nodeList]; // convert nodeList to array

const nodeListArray = Array.from(nodeList); // convert nodeList to array