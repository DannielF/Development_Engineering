document.getElementById();

parent.getElementsByTagName();

parent.getElementByClassName();

"-------------------------------------------------------------------------------------"

parentElement.querySelector("")
/**
 * tag => "div", "section"
 * class => ".class1"
 * Id => "#id"
 */

parentElement.querySelectorAll();
/**
 * @querySelectorAll()
 * Returns a NodeList of all the elements in the document that match the specified group of selectors.
 */