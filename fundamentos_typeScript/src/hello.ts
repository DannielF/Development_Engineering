console.log('Hello TypeScript');

/**
 * cheat sheet
 *
 * tsc  -> compile all files
 * tsc hello.ts
 * tsc hello.ts --watch
 * tsc --init (create tsconfig.json)
 * tsc --project nameProject  -> run tsconfig.json in this project
 */
