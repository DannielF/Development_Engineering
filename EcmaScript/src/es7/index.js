// include trabaja sobre un arreglo o string
// nos permite sabes si hay un elemento dentro de este valor

let numbers = [1, 2, 4, 7, 8];

if (numbers.includes(7)) {
  console.log('we found number 7 in the array');
} else {
  console.log('we can´t find anything');
}

// elevar a la potencia

let base = 4;
let exponent = 3;
let result = base ** exponent;

console.log(result);
